import styled from 'styled-components'

const DefaultStyledPageContainer = styled.div`
    margin: 0 auto 0 auto;
    padding: 0;
    max-width: 1200px;
    text-align: center;
    position: relative;
`

const StyledPageContainer = DefaultStyledPageContainer

export default StyledPageContainer
